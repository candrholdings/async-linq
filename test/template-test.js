!function (assert, vows, linq, util) {
    'use strict';

    vows.describe('linq.xxx.array').addBatch({
    }).export(module);

    vows.describe('linq.xxx.map').addBatch({
    }).export(module);
}(require('assert'), require('vows'), require('../linq3'), require('../test-util'));